name: Build QMK Firmware
on:
  schedule:
  - cron: '0 12 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container: qmkfm/qmk_cli
    strategy:
      fail-fast: false
      matrix:
        keymap:
        - via
        - default
        - vial

    steps:
    - name: Checkout QMK
      if: ${{ (matrix.keymap != 'vial') }}
      uses: actions/checkout@v3
      with:
        repository: qmk/qmk_firmware
        ref: develop
        submodules: recursive

    - name: Checkout QMK vial fork
      if: ${{ (matrix.keymap == 'vial') }}
      uses: actions/checkout@v3
      with:
        repository: vial-kb/vial-qmk
        submodules: recursive

    - name: Build ${{ matrix.keymap }} keymaps
      continue-on-error: true
      run: qmk multibuild -j3 -km ${{ matrix.keymap }}

    - name: Sort output
      shell: bash
      run: |
        for file in *.hex *.bin *.uf2; do
          DIR=fw_output/$(echo $file | cut -d'_' -f1)
          [ -f "$file" ] && (mkdir -p "$DIR"; mv -v "$file" "$DIR/") || true
        done

    - name: Archive ${{ matrix.keymap }} firmware
      uses: actions/upload-artifact@v3
      continue-on-error: true
      with:
        name: fw_output
        path: fw_output/
        retention-days: 1 

  upload:
    needs: build
    uses: filterpaper/scripts/.github/workflows/commit-artifact.yml@main
    with:
      artifact_name: fw_output
      artifact_folder: .

  purge:
    needs: build
    uses: filterpaper/scripts/.github/workflows/delete-workflow.yml@main

